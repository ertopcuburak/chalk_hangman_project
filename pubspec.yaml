name: chalk_hangman
description: Turkish Chalkboard Hangman Game
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2

flutter:
  uses-material-design: true

  assets:
    - assets/words/
    - assets/images/